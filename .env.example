# Plotline Configuration
# Copy to .env and fill in values

# Optional: Anthropic API key for AI script organization
# Without this, Plotline uses rule-based organization (still works great)
ANTHROPIC_API_KEY=

# Server port (default: 3847)
PLOTLINE_PORT=3847
